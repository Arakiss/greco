# Index

The active production limit lives in `src/settings.rs` on the line containing
`ACTIVE_LIMIT`.

Required value for this task: `64`.

The generated dump in `generated/tenant_snapshot.rs` is intentionally noisy and
does not contain the source of truth for this change.
