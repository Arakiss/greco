# T4 Movability

Friction profile: objective failure and retracements caused by broad staging
commands that include unrelated dirty files.

Hypothesis: a Layer A cached procedure that enumerates changed paths and stages
only explicit task paths should increase objective success and reduce
over-staging failures.
